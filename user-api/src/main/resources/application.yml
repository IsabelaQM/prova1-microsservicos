spring:
  data:
    mongodb:
      uri:
      database: user-api
logging:
  level:
    org:
      springframework:
        data:
          mongodb:
            core:
              MongoTemplate: DEBUG